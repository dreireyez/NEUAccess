
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the NEU Library Visitor App, storing their profile information and access rights.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity, corresponding to the Firebase Authentication UID."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, strictly enforced to end with '@neu.edu.ph'.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the application, determining their access level. Possible values are 'admin', 'staff', or 'user' (default 'user')."
        },
        "college": {
          "type": "string",
          "description": "The academic college or school the user belongs to. This field is nullable (can be null) until selected during the onboarding process."
        },
        "isBlocked": {
          "type": "boolean",
          "description": "A flag indicating whether the user's access to the application is currently blocked by an administrator (default false)."
        }
      },
      "required": [
        "id",
        "email",
        "role",
        "isBlocked"
      ]
    },
    "Visit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Visit",
      "type": "object",
      "description": "Records an instance of a user's visit to the NEU Library, including time-in, time-out, and multiple purposes.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Visit entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who made the visit."
        },
        "timeIn": {
          "type": "string",
          "description": "The timestamp when the user timed in.",
          "format": "date-time"
        },
        "timeOut": {
          "type": "string",
          "description": "The timestamp when the user timed out (signed out).",
          "format": "date-time"
        },
        "reasons": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of purposes selected for the visit."
        },
        "status": {
          "type": "string",
          "enum": ["active", "completed"],
          "description": "The current status of the visit."
        }
      },
      "required": [
        "id",
        "userId",
        "timeIn",
        "reasons",
        "status"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles."
        }
      },
      {
        "path": "/visits/{visitId}",
        "definition": {
          "entityName": "Visit",
          "schema": {
            "$ref": "#/backend/entities/Visit"
          },
          "description": "Records individual library visits with time-in/out and multi-select reasons."
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Admin role lookup."
        }
      },
      {
        "path": "/roles_staff/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Staff role lookup."
        }
      }
    ]
  }
}
